import Link from "next/link";import { ArrowLeft,Bell,FileText,Image as ImageIcon,ListChecks,MessageSquare,Users } from "lucide-react";import { AppNav } from "@/components/app-nav";
const items=[[MessageSquare,"Genel","Ekip iletişim alanı"],[Bell,"Duyurular","Güncel bildirimler"],[Users,"Personeller","İstasyon ekibi"],[ListChecks,"Görevler","Görev takibi"],[ImageIcon,"Fotoğraflar","Saha görselleri"],[FileText,"Dosyalar","Ortak belgeler"]] as const;
export default async function Station({params}:{params:Promise<{stationSlug:string}>}){const{stationSlug}=await params;const name=stationSlug.split("-").slice(1).join(" ");return <main className="shell"><header className="flex items-center gap-4"><Link href="/dashboard" className="rounded-2xl border border-zinc-800 p-3"><ArrowLeft/></Link><div><p className="eyebrow">İstasyon çalışma alanı</p><h1 className="text-2xl font-bold capitalize">{name}</h1></div></header><div className="mt-10 grid gap-4 sm:grid-cols-2 lg:grid-cols-3">{items.map(([Icon,title,text],i)=><section key={title} className="glass rounded-3xl p-5"><span className={`grid h-12 w-12 place-items-center rounded-2xl ${i===0?"bg-red-600":"bg-zinc-800"}`}><Icon/></span><h2 className="mt-6 text-lg font-bold">{title}</h2><p className="muted mt-1 text-sm">{text}</p><p className="mt-5 text-xs font-semibold text-rose-400">Yakında</p></section>)}</div><AppNav/></main>}
