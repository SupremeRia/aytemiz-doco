import { updateOwnProfile } from "@/app/profile/actions";import { Button,Input,Textarea } from "@/components/ui/primitives";
export function ProfileForm({profile}:{profile:{phone:string|null;address:string|null;emergency_phone:string|null}}){return <form action={updateOwnProfile} className="form-stack"><div className="form-grid"><label><span>Telefon</span><Input name="phone" inputMode="tel" autoComplete="tel" defaultValue={profile.phone??""} maxLength={30}/></label><label><span>Acil durum telefonu</span><Input name="emergencyPhone" inputMode="tel" autoComplete="tel" defaultValue={profile.emergency_phone??""} maxLength={30}/></label></div><label><span>Adres</span><Textarea name="address" autoComplete="street-address" defaultValue={profile.address??""} maxLength={500}/></label><p className="muted">Rol, istasyon, sicil ve çalışma durumu yalnızca yetkili yöneticiler tarafından değiştirilebilir.</p><Button>Profili kaydet</Button></form>}
