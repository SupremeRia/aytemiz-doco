"use client";
import { useState } from "react";import { Button,Input } from "@/components/ui/primitives";import { createClient } from "@/lib/supabase/client";
export function EmailForm({currentEmail}:{currentEmail:string}){const[busy,setBusy]=useState(false),[message,setMessage]=useState("");async function submit(event:React.FormEvent<HTMLFormElement>){event.preventDefault();setBusy(true);setMessage("");const email=String(new FormData(event.currentTarget).get("email")??"").trim().toLowerCase();const{error}=await createClient().auth.updateUser({email},{emailRedirectTo:`${location.origin}/auth/callback?next=/profile`});setMessage(error?"E-posta değişikliği başlatılamadı.":"Doğrulama bağlantısı yeni e-posta adresine gönderildi.");setBusy(false)}return <form onSubmit={submit} className="form-stack"><label><span>Yeni e-posta</span><Input name="email" type="email" defaultValue={currentEmail} autoComplete="email" required/></label><Button disabled={busy}>{busy?"Gönderiliyor…":"E-posta değişikliğini doğrula"}</Button><p className="muted" role="status">{message||"E-posta, doğrulama tamamlandıktan sonra profilinizle otomatik eşitlenir."}</p></form>}
