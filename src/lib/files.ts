import {createClient} from "@/lib/supabase/server";
export type StationFile={id:string;name:string;description:string|null;category:string;created_at:string;creator_name:string;media_id:string;bucket:string;storage_path:string;original_name:string;mime_type:string;size_bytes:number;can_delete:boolean;url?:string};
export async function listStationFiles(stationId:string,options:{q?:string;category?:string;cursorCreated?:string;cursorId?:string}={}){const supabase=await createClient();const{data,error}=await supabase.rpc("list_station_files",{target_station:stationId,search_text:options.q??null,category_filter:options.category??null,cursor_created:options.cursorCreated??null,cursor_id:options.cursorId??null,page_size:20});const items=await Promise.all(((data??[]) as StationFile[]).map(async item=>{const{data:signed}=await supabase.storage.from(item.bucket).createSignedUrl(item.storage_path,900,{download:false});return{...item,url:signed?.signedUrl}}));const{data:canUpload}=await supabase.rpc("can",{permission_slug:"files.upload",check_station:stationId});return{items,error,canUpload:canUpload===true};}
