import "server-only";
import webpush from "web-push";
import {createClient} from "@supabase/supabase-js";

const pushTypes=["task_assigned","task_rejected","task_approved","task_review","announcement_created","handover_submitted","critical_issue","issue_assigned"];
export async function dispatchEntityPush(entityType:string,entityId:string){const url=process.env.NEXT_PUBLIC_SUPABASE_URL,key=process.env.SUPABASE_SERVICE_ROLE_KEY,publicKey=process.env.NEXT_PUBLIC_VAPID_PUBLIC_KEY,privateKey=process.env.VAPID_PRIVATE_KEY,subject=process.env.VAPID_SUBJECT;if(!url||!key||!publicKey||!privateKey||!subject)return{sent:0,configured:false};webpush.setVapidDetails(subject,publicKey,privateKey);const admin=createClient(url,key,{auth:{persistSession:false,autoRefreshToken:false}});const{data:notifications}=await admin.from("notifications").select("user_id,title,body,action_url,severity").eq("entity_type",entityType).eq("entity_id",entityId).in("type",pushTypes);if(!notifications?.length)return{sent:0,configured:true};const users=[...new Set(notifications.map(item=>item.user_id))],{data:subscriptions}=await admin.from("push_subscriptions").select("id,user_id,endpoint,p256dh,auth").in("user_id",users).is("revoked_at",null);let sent=0;for(const subscription of subscriptions??[]){const notification=notifications.find(item=>item.user_id===subscription.user_id);if(!notification)continue;try{await webpush.sendNotification({endpoint:subscription.endpoint,keys:{p256dh:subscription.p256dh,auth:subscription.auth}},JSON.stringify({title:notification.title,body:notification.body,url:notification.action_url,severity:notification.severity}),{TTL:3600,urgency:notification.severity==="urgent"?"high":"normal"});sent++;await admin.from("push_subscriptions").update({last_used_at:new Date().toISOString()}).eq("id",subscription.id)}catch(error){const status=(error as {statusCode?:number}).statusCode;if(status===404||status===410)await admin.from("push_subscriptions").update({revoked_at:new Date().toISOString()}).eq("id",subscription.id)}}return{sent,configured:true}}
