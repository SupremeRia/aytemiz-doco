import {createClient} from "@/lib/supabase/server";
export type DirectoryUser={user_id:string;display_name:string;email:string|null;status:string;roles:string[];stations:{id:string;name:string;city:string}[];regions:string[]};
export type OperationPost={id:string;title:string;description:string|null;status:string;created_at:string;creator_name:string;creator_roles:string[];station_id:string;station_name:string;station_slug:string;shift_name:string|null;participants:{id:string;name:string}[];media:{id:string;bucket:string;path:string;name:string;mime:string;url?:string}[];can_edit:boolean;can_delete?:boolean};
async function signed<T extends {media:{bucket:string;path:string}[]}>(supabase:Awaited<ReturnType<typeof createClient>>,items:T[]){return Promise.all(items.map(async item=>({...item,media:await Promise.all(item.media.map(async media=>{const{data}=await supabase.storage.from(media.bucket).createSignedUrl(media.path,3600);return{...media,url:data?.signedUrl}}))})));}
export async function getDirectory(stationId?:string,q?:string){const supabase=await createClient();const{data}=await supabase.rpc("search_user_directory",{search_text:q??null,target_station:stationId??null,cursor_name:null,cursor_id:null,page_size:50});return(data??[]) as DirectoryUser[];}
export async function getOperationFormData(stationId:string){const supabase=await createClient();const[people,shifts,permission]=await Promise.all([getDirectory(stationId),supabase.from("shifts").select("id,name,starts_at,ends_at").eq("is_active",true).order("sort_order"),supabase.rpc("can",{permission_slug:"status_posts.create",check_station:stationId})]);return{people,shifts:shifts.data??[],canCreate:permission.data===true};}
export async function listOperationPosts(options:{stationId:string;status?:string;q?:string;from?:string;to?:string;shift?:string;person?:string;quick?:string;cursorCreated?:string;cursorId?:string}){let from=options.from;if(!from&&options.quick){const now=new Date();if(options.quick==="today")from=now.toISOString().slice(0,10);if(options.quick==="week")from=new Date(now.getTime()-6*86400000).toISOString().slice(0,10);if(options.quick==="month")from=new Date(now.getFullYear(),now.getMonth(),1).toISOString().slice(0,10)}const supabase=await createClient();const{data,error}=await supabase.rpc("list_operation_posts",{target_station:options.stationId,status_filter:options.status??"published",search_text:options.q??null,date_from:from??null,date_to:options.to??null,target_shift:options.shift??null,target_person:options.person??null,cursor_created:options.cursorCreated??null,cursor_id:options.cursorId??null,page_size:12});return{items:await signed(supabase,(data??[]) as OperationPost[]),error};}
export async function getOperationPost(id:string){const supabase=await createClient();const{data,error}=await supabase.rpc("get_operation_post",{target_post:id});if(!data)return{post:null,error};const[post]=await signed(supabase,[data as unknown as OperationPost]);return{post,error};}
